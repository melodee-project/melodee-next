# Melodee Environment Configuration
# Production-ready settings

# Database credentials
MELODEE_DB_PASSWORD=secure_password_change_me
POSTGRES_PASSWORD=secure_password_change_me

# JWT Secret (should be a strong, random value)
MELODEE_JWT_SECRET=very_strong_secret_key_that_should_be_changed_in_production_and_must_be_at_least_32_characters_long

# Redis configuration
REDIS_PASSWORD=

# Grafana admin password
GRAFANA_ADMIN_PASSWORD=admin123

# Storage paths (for development/testing)
STORAGE_MOUNT=./volumes/storage
INBOUND_MOUNT=./volumes/inbound
STAGING_MOUNT=./volumes/staging
QUARANTINE_MOUNT=./volumes/quarantine
USER_IMAGES_MOUNT=./volumes/user_images

# External API Keys (if needed)
LASTFM_API_KEY=
MUSICBRAINZ_API_KEY=
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=